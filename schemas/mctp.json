{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$defs": {
        "MCTPI2CTarget": {
            "properties": {
                "Address": {
                    "description": "The physical address for the device on the I2C bus.",
                    "type": "string"
                },
                "Bus": {
                    "description": "The bus index to which the device is attached",
                    "type": "string"
                },
                "Name": {
                    "description": "A recognisable name for the device",
                    "type": "string"
                },
                "StaticEndpointID": {
                    "description": "Assign the provided endpoint ID to the peripheral device and exclude the EID from the dynamic allocation pool. If StaticEndpointID is not defined then the device will be allocated an endpoint ID from the dynamic pool.",
                    "type": "string"
                },
                "BridgePoolStartEID": {
                    "description": "Assigns the endpoint ID for the bridge pool to start with. If BridgePoolStartEID is set to 0, it will be disabled automatically.",
                    "type": "string"
                },
                "Type": {
                    "type": "string",
                    "enum": ["MCTPI2CTarget"]
                }
            },
            "required": ["Address", "Bus", "Name", "Type"],
            "type": "object"
        },
        "MCTPUSBTarget": {
            "properties": {
                "Name": {
                    "description": "A recognisable name for the device",
                    "type": "string"
                },
                "Type": {
                    "type": "string",
                    "enum": ["MCTPUSBTarget"]
                },
                "Interface": {
                    "description": "A recognisable interface name for the device",
                    "type": "string"
                },
                "USBPath": {
                    "description": "Unique usb device path for visible under usb tree <busID-port1.port2.port3…>",
                    "type": "string"
                },
               "StaticEndpointID": {
                   "description": "Assign the provided endpoint ID to the peripheral device and exclude the EID from the dynamic allocation pool. If StaticEndpointID is not defined then the device will be allocated an endpoint ID from the dynamic pool.",
                   "type": "string"
               },
              "BridgePoolStartEID": {
                   "description": "Assigns the endpoint ID for the bridge pool to start with. If BridgePoolStartEID is set to 0, it will be disabled automatically.",
                   "type": "string"
               },
               "IgnoreEIDs": {
                    "description": "A comma separated list of endpoint IDs to be ignored by BusOwner.",
                    "type": "string"
                }
            },
            "required": ["Name", "Type", "Interface", "USBPath"],
            "type": "object"
        },
        "MCTPSPIDevice": {
            "properties": {
                "Name": {
                    "description": "A recognisable name for SPI device",
                    "type": "string"
                },
                "Type": {
                    "type": "string",
                    "enum": ["MCTPSPIDevice"]
                },
                "Bus": {
                    "description": "A recognisable bus number on which SPI device is attached",
                    "type": "string"
                },
                "ChipSelect": {
                    "description": "A recognisable chip selecet pin where SPI device is attached",
                    "type": "string"
                },
                "StaticEndpointID": {
                    "description": "Assign the provided endpoint ID to the peripheral device and exclude the EID from the dynamic pool allocation. If StaticEndpointID is not defined then the device will be allocated an endpoint ID from the dynamic pool.",
                    "type": "string"
                }
            },
            "required": ["Name", "Type", "Bus", "ChipSelect"],
            "type": "object"
        },
        "MCTPI3CTarget": {
            "properties": {
                "Address": {
                    "description": "The 6-byte PID for the device on the I3C bus",
                    "type": ["string", "array"]
                },
                "Bus": {
                    "description": "The bus index to which the device is attached",
                    "type": "string"
                },
                "Name": {
                    "description": "A recognisable name for the device",
                    "type": "string"
                },
                "StaticEndpointID": {
                    "description": "Assign the provided endpoint ID to the peripheral device and exclude the EID from the dynamic pool allocation. If StaticEndpointID is not defined then the device will be allocated an endpoint ID from the dynamic pool.",
                    "type": "string"
                },
                "BridgePoolStartEID": {
                    "description": "Assigns the endpoint ID for the bridge pool to start with. If BridgePoolStartEID is set to 0, it will be disabled automatically.",
                    "type": "string"
                },
                "Type": {
                    "type": "string",
                    "enum": ["MCTPI3CTarget"]
                }
            },
            "required": ["Address", "Bus", "Name", "Type"],
            "type": "object"
        }
    }
}
